# C8 desktop evidence session (DESKTOP_EVIDENCE_SESSION v2) — artifact pointer

- Executed 2026-08-17 (15:02Z–18:00Z) on branch `evidence/c8-20260817` (base f4e1652); spec `docs/icra27/DESKTOP_EVIDENCE_SESSION.md` (sha256 548380c6…).
- Artifact root: `/home/moksh/schurvio-icra27-artifacts/evidence-c8/c8-20260817T150227Z` — `SESSION_SUMMARY.md`, `RUN_LOG.md`, `DECISIONS.md` (D-C8-1..7), `aggregate/` (Item 1 H4 matrix), `item2/` (stall anatomy), `item3/paper-numbers/` (regeneration + ledger), `cells/` (182 shim runs).
- Item 1 (ledger F4): H4 all-zero violation columns TRUE; inertness 22/22 BYTE_IDENTICAL; classes 1-6,8 realized 1111–1263 faults each over 63,779 update invocations (0 multi-commit, 0 escaped exceptions, 0 state writes on realized transaction faults); class 7: 22/22 console-failure runs + 116/116 T0-stream fault runs outcome-unaffected; natural typed failures 0 across CDSC-1R4/PERTURB-1/ABLATE-REC-1/BLACKOUT-1 (324 console logs, 993,220 state rows; 253,222 ordinary chi-square gate rejections). Tooling: `tools/c8_faultinj/` (LD_PRELOAD shim on the frozen binary), `scripts/icra27/c8_fault_driver.py`, `c8_fault_aggregate.py`.
- Item 2 (F14): `item2/stall_anatomy.md` — premise correction: the natural rotation event is frame-ABSENT; recOFF resumes at the first post-gap pair (rounding-only TRACKING_LOSS); U0 aborts at the gap exit (5/5); recON re-anchors; mechanism value = post-gap accuracy (4.2577 m vs 0.0878 m ATE at ~86 % coverage).
- Item 3: `make paper-numbers` / `make paper-numbers-check` (root Makefile; `scripts/icra27/paper_numbers.py`; golden hashes `project/evidence/c8/paper_numbers_GOLDEN.sha256.json`). Frozen aggregators regenerate from tooling commits (golden PASS x4 modulo generated_utc); 326 cells; 6 pre-BLACKOUT frozen->QA flips (all ABLATE-REC-1 rotation recOFF).
- Hard rules: estimator diff empty (f4e1652..HEAD over ov_msckf ov_core ov_init ov_eval config); frozen binary sha256 unchanged (0e46fa3e…).
